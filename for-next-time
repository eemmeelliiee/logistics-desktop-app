
hur ersötta forceupdateofobservablelist?


glöm inte att make sure alla fält nollställs vid buttonclick!!

---------------HAM------------------
överblicka kod, få grepp
strunta i GUI nu
<!-- skapa handler för inspectionLog (varje klass har en lista,
    WarehouseHandler har en lista av warehouses, osv.) 
    GÖR TESETER (ny testklass för varje handler, med beforeeach, glöm ej att kasta exeptions)
    skapa instancer av dessa i DataManager 
    skapa ALLA CRUD-funktioner för dessa i DataManager (samt först respektive Handler-klass) (för att centralisera alla UI-funktioner som behövs för att hantera dessa)
        vilket fel kan hända i en CRUD-funktion= lägg till error handling. -->
            (när en CRUD-funktion sker i en klass, hur kan detta påverka andra??*)
        se till att forceUpdateOfObservableList() finns i varje handler, i update-funktionerna!
        <!-- ändra endast värden genom DataManager, inte på handlers (DataManager.updateShipmentId, inte shipmentHandler.updateShipmentId) -->
skapa en DataService-klass, inga attributes, bara funktioner som använder DataManager-funktioner
    kommer vilja använda funktioner som t.ex DataManager.getInstance().getShipmentHandler().getShipments()
        (eller i konstruktorn, ha en instans av DataManager (för att kunna använda dess funktioner) ??)
    alla funktioner som på något sätt kräver en kombination av flera klasser - skönt att kunna printa saker och visa logiska (matte)-grejer
    och som inte kommer påverka något t.ex.
    - getAllShipmentLogsForWarehouse
    - getCurrentShipmentsInWareHouse

    - getAllShipmentLogsForShipment

<!-- MAKE SURE TO UPDATE WAREHOUSES ANS SHIPMENTS IN DATAMANAGER FOR CRUD_FUNCTIONS THAT NEED IT -->

    glöm ej 14 dagar => setLabel

*removeShipment kommer radera de shipmentLogs med det id:t osv

sen kolla på GUI !!!
hur göra för att byta tabs?

 behöver inte ha datamaneger static i dataservice - egentiligen inget object överhuvudtaget pga den är statisk

1. handlers statiska så alla refernenser till datamangee försvinner i dataservice
2. få kod funka
2. sen resten mer refreshvalues i warehuse och datamanager

skulle  göra alla handler klasser statiska 
och dataservice klassen statisk

byta namn på datamanager till crud-service

byta namn på dataservice till analyticsservice
så att man går från att tänka på att båda 

sen se till alla anrop i fd datascervice går genom de statiska hanlderna istället

alla updatemetoderna som finns i dataservice - byt namn till get (getMostRecentDate)
en metod i warehouse refreshvalues som anropar alla update metoder i dataservice
hämtar värdet och själv sätter sitt värde till det

analyticsservice ska inte påverka tillstånd på nåt sätt
utan warehouse t.ex setter sina egna värden till de värdena som hämtas från analyticsservice

varje gång nåt har skapats/tagits bort/ där jag nu anropar updatewarehouseshipmentinformation
istället för warehouse.refreshvalues (refreshshipmentwalues och refresh inspectionvalues) (två refresh bättre än 1 refreshvalues)
refreshvalues i warehouse
kommer get alla värden get current stockleve
och sätta sit egget stocklevel attribut till getstocklevel

om jag vill få det att funka !!!!!!!!!
- gör hannlder statiska så alla refernenser till datamangee försvinner i dataservice

hur testa? använda datamagener för stt skapa massa wareohse, shipments osv
du får ju ut de objektenm datamanager returnerar ju objekt

datamanger deletwarehosuse

istället för att sätta i dataervice så return objekt i updateblablabla


för då kommer man inte behöva gå genom datamanager i dataservice

updatewarehouseinformation ersätts av refreshvaluesför warehouse

extra, ej ndövändigt:
servie´ce och hanlders i eget paket
service klasser som public (public class service)
handlersklasser inte public (class hanlder)
kommer bara kunna anropa serviceklasser utanför paketet

alla handlers ska ha en cleardata ()-metod 