Nästa gång:

 Innehåller Shipment CRUD-function ??? (samt resterande klasser)
2. Fortsätt med funktioner för alla klasser
3. Kolla upp error handling

hur snygga till formatering?

HUR:
if the inspectionentry is the only inspection, set inspected to false
public void removeInspection(InspectionLog inspection) {
    if (inspection.getShipment().getInspected()) {
        inspectionsMade.remove(inspection);
        System.out.println("Inspection removed from shipment " + inspection.getShipment().getShipmentId());
    }
    System.out.println("Shipment has not been inspected yet");

}

if i try to add a shipmentlog to a warehouse that doesnt exist?

glöm inte att make sure alla fält nollställs vid buttonclick!!

if warehouse name matches list of warehouses existing in the system, add shipmentlog to warehouse
else, print error message


---------------HAM------------------
skapa handlers för warehouse, shipmentlog, inspectionLog
    skapa instancer av dessa i DataManager 
    skapa CRUD-funktioner för dessa i DataManager (samt först respektive Handler-klass) (för att centralisera alla UI-funktioner som behövs för att hantera dessa)
skapa en DataService som har
    alla funktioner som på något sätt kräver en kombination av flera klasser. t.ex.
    - getAllShipmentLogsForWarehouse
    - getAllShipmentsInWareHouse
    
    - getAllShipmentLogsForShipment